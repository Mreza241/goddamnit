name: Run Meli Action

on:
  push:
    branches:
      - main

jobs:
  run-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Meli Action
        uses: Kurdeus/Meli-Action@main
